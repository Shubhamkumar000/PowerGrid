import React from 'react';
import { SettingsPage } from '../components/settings/SettingsPage';
import { Tabs, TabsList, TabsTrigger, TabsContent } from "../components/settings/TabComponents"

export const Settings = () => {
  return <SettingsPage />;
};

